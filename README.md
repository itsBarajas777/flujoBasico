# #Practica 1 - Flujo Basico de git y github 
repositorio para practicar el flujo basico de git y github
```sh
   git status
     git add .
     git status
     git commit -m "Descripcion del cambio que hiciste en el archivo README.md"
     git push